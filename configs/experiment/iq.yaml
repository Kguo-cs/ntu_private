# @package _global_

defaults:
  # - override /trainer: ddp
  - override /model: smart_iq

model:
  model_config:
    lr: 5e-4
    lr_min_ratio: 1e-2
    token_processor:
      map_token_sampling: # open-loop
        num_k: 1 # for k nearest neighbors
        temp: 1.0 # uniform sampling
      agent_token_sampling: # closed-loop
        num_k: 1 # for k nearest neighbors
        temp: 1.0
      pred_entry: True


    training_rollout_sampling:
      criterium: topk_prob # {topk_dist_sampled_with_prob, topk_prob, topk_prob_sampled_with_dist}
      num_k: 2048 # for k nearest neighbors, set to -1 to turn-off closed-loop training
      temp: 1.0
    validation_rollout_sampling:
      criterium: topk_prob
      num_k: 2048 # for k most likely
      temp: 1.0
    val_open_loop: False
    #val_closed_loop: False
    #finetune: true
    smart: false
#    n_vis_batch: 1
#    n_vis_scenario: 1
#    n_vis_rollout: 1
#    n_rollout_closed_val: 1

#ckpt_path : '/home/ke/code/catk/src/waymo_data/bc40_para_typeshapepos05_head128_epoch=3-step=46261.ckpt'

task_name: bc40_para_typeshapepos05_head128_nooffset #_0pospara_typeshapeoffset #cross_prev_query0_head64 #_nodetach #all_para #cross_prev_0pos  #cross_sa #_pos256 #_a_noexit #_cross_sa #_currentvalid #paraentry_sa #currentvalid #current #_exitpred1_clone_allnoentry_sa_all #token10150 #_a2map_notokenfeata #_layer3__tokenexitexit #_14679 #_token_exit #_token #14679_tokenmask #_notokenmask_goal

trainer:
  limit_train_batches: 1.0
  limit_val_batches: 1
  check_val_every_n_epoch: 1
  val_check_interval: 0.1
  #val_check_interval: 0.0000002
  max_epochs: 64
  #gradient_clip_val: 0.0

model_checkpoint:
  _target_: lightning.pytorch.callbacks.ModelCheckpoint
  dirpath: null     # or any directory you prefer
  filename: "epoch={epoch}-valmeta={val_closed/wosac_likelihood/metametric:.4f}"
  monitor: "val_closed/wosac_likelihood/metametric"   # <== key metric from validation
  verbose: false
  save_last: true
  save_top_k: 3
  mode: "max"                          # "max" since your metric increases with performance
  auto_insert_metric_name: false
  save_weights_only: false
  every_n_train_steps: null
  train_time_interval: null
  every_n_epochs: null
  save_on_train_epoch_end: false       # <== important, disables save after training epoch

data: 
  train_batch_size: 40
  val_batch_size: 1
  test_batch_size: 10
  num_workers: 1

#action: validate
#action: finetune
# nohup python run.py > 1.txt 2>&1 &
