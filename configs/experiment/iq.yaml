# @package _global_

defaults:
  # - override /trainer: ddp
  - override /model: smart_iq

model:
  model_config:
    lr: 5e-4
    lr_min_ratio: 1e-2
    token_processor:
      map_token_sampling: # open-loop
        num_k: 1 # for k nearest neighbors
        temp: 1.0 # uniform sampling
      agent_token_sampling: # closed-loop
        num_k: 1 # for k nearest neighbors
        temp: 1.0
    training_rollout_sampling:
      criterium: topk_prob # {topk_dist_sampled_with_prob, topk_prob, topk_prob_sampled_with_dist}
      num_k: 2048 # for k nearest neighbors, set to -1 to turn-off closed-loop training
      temp: 1.0
    validation_rollout_sampling:
      criterium: topk_prob
      num_k: 2048 # for k most likely
      temp: 1.0
    val_open_loop: False
    #val_closed_loop: False
    #finetune: true

ckpt_path : '~/code/sim/src/logs/bc32_edge1_d30_l4/2025-09-03_12-57-40/bc/7ooulrxz/checkpoints/epoch=26-step=410913.ckpt' #'./ckpts/bc32_edge1_pt30_inter2_epoch=24-step=380475_0.7854.ckpt' #

trainer:
  limit_train_batches: 1.0
  limit_val_batches: 1
  check_val_every_n_epoch: 1
  #val_check_interval: 0.00002
  max_epochs: 32
  #gradient_clip_val: 0.0

data: 
  train_batch_size: 32
  val_batch_size: 32
  test_batch_size: 10
  num_workers: 32

#action: validate
#action: finetune
# nohup python run.py > 1.txt 2>&1 &
