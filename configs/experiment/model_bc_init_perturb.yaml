# @package _global_

defaults:
  - override /model: smart_fast

model:
  model_config:
#    lr: 1e-4
#    lr_min_ratio: 0.05
    token_processor:
      pred_init: True

    finetune: true
    #n_batch_wosac_metric: 0

ckpt_path: "./waymo_data/xflow512_n30.ckpt"

task_name: model40_resample_less2_prob1 #perturb5e4 #less

trainer:
  #val_check_interval: 0.0000002
  max_epochs: 16

data:
  train_batch_size: 40 #6
  val_batch_size: 256
  train_raw_dir: ${paths.cache_root}/training_map2_a_light

action: finetune
