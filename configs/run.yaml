# @package _global_

# specify here default configuration
# order of defaults determines the order in which configs override each other
defaults:
  - _self_
  - data: waymo
  - model: smart
  - callbacks: default
  - logger: wandb
  - trainer: default
  - paths: default
  - hydra: default

  # experiment configs allow for version control of specific hyperparameters
  # e.g. best hyperparameters for given model and datamodule
  - experiment: iq_finetune #wosac_sub #iq_finetune  #

action: fit # fit, finetune, validate, test

# task name, determines output directory path
task_name: AIRL64_value0001_pred_expertvalid_0001colpredtrainpolicy #mask #expertcolpolicy #learnvalue001 #_learnmap_ #coloffpolicy #_learnmap_allq #@_share2 #_dlatent #bc32_map2_CVAE #edge1_inter2_pt20 #AIRL64_edge1_share2_dislatent #kl1_nomap #vae_nosharelatent #bc32_edge1_inter2_pt20 #AIRL64_edge1_vae_nosharelatent #klr09999_nomap #bc32_edge1_inter2_vae #AIRL64_edge1_klr1_nomap #klr1_no #map2_guassian2_klnorm #kl1_learnmap #kl10_nobc #sharelatent2 #bc32_histdrop01_03_formert

# simply provide checkpoint path to resume training
ckpt_path: null
train_log_dir: null

# seed for random number generators in pytorch, numpy and python.random
seed: 817

cfg: null
