# @package _global_

# specify here default configuration
# order of defaults determines the order in which configs override each other
defaults:
  - _self_
  - data: waymo
  - model: smart
  - callbacks: default
  - logger: wandb
  - trainer: default
  - paths: default
  - hydra: default

  # experiment configs allow for version control of specific hyperparameters
  # e.g. best hyperparameters for given model and datamodule
  - experiment: iq #_val #iq #ego_gmm_pre_bc  #wosac_sub #iq #iq

action: fit # fit, finetune, validate, test

# task name, determines output directory path
task_name: AIRL64_map2_agentall_sa #_Aedge0102 #anorm_anorm #light01 #_sharelight #share_sharedis #bc64_map2 #AIRL64_map2_sharedis #bc64_map2 #AIRL64_map2 #bc64_map10 #2_vis #bc48_map2_adamw #_vis #AIRL64_stateLogpdetach_near #mask #3 #bc64_map2_vis


# simply provide checkpoint path to resume training
ckpt_path: null
train_log_dir: null

# seed for random number generators in pytorch, numpy and python.random
seed: 817

cfg: null
