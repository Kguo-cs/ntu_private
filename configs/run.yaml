# @package _global_

# specify here default configuration
# order of defaults determines the order in which configs override each other
defaults:
  - _self_
  - data: waymo
  - model: smart
  - callbacks: default
  - logger: wandb
  - trainer: default
  - paths: default
  - hydra: default

  # experiment configs allow for version control of specific hyperparameters
  # e.g. best hyperparameters for given model and datamodule
  - experiment: iq_finetune #iq_finetune  #wosac_sub

action: fit # fit, finetune, validate, test

# task name, determines output directory path
task_name: AIRL64_drop075_valuemlp #detach #bc32_histdrop01_03_l14 #_t20 #AIRL64_dis05_inter1detach_nextA #_ent01value #_3e5 #AIRL128_dis075d01_ent003_3e5_nei3 #bc32_histdrop01_03_l4 #AIRL64_dis075d10_ent01_3e5 #pugail_dr20 #bc32_histdrop01_03_t60_drop02 #AIRL128_normreturn_grad0_dis02

# simply provide checkpoint path to resume training
ckpt_path: null
train_log_dir: null

# seed for random number generators in pytorch, numpy and python.random
seed: 817

cfg: null
