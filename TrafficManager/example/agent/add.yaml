agent:
  add:
    - type: 'bicycle'  #pedestrian,bicycle,car,truck
      id: 1000
      position: [0,200]
      goal: #[5,200]   # 可选
      heading: 1.57    # 可选, 可根据goal生成
      speed:  #可选, 可根据type生成
      shape: #可选, 可根据type生成